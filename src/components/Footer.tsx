import { Box} from '@mui/material';

const Footer = () => {
    return (

        <Box
            sx={{
                width: '100%',
                height: '60px',
                backgroundColor: '#bd3233',
                color: 'white',
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center',
                position: 'fixed',
                bottom: 0,
                left: 0,
            }}
        >

        </Box>
    );
};

export default Footer;
